/**
 * Basic user information used across multiple endpoints.
 */
export interface MiniUser {
  /**
   * User's email address.
   */
  email: string;

  /**
   * User's full name.
   */
  name: string;
}
